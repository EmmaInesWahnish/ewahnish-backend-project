const renderModalDeleteFromCart = (productId, cartId) => {

    console.log(`Aqui se eliminaría el producto con id ${productId} del carrito con id ${cartId}`)

}

export default renderModalDeleteFromCart;